import { Construct } from 'constructs';

/**
 * Properties for RemotionLambdaFunction construct.
 * To be implemented in issue #2.
 */
export interface RemotionLambdaFunctionProps {
  // placeholder
}

/**
 * CDK Construct that provisions the Remotion Lambda render function.
 */
export class RemotionLambdaFunction extends Construct {
  constructor(scope: Construct, id: string, _props?: RemotionLambdaFunctionProps) {
    super(scope, id);
    // implementation pending
  }
}
